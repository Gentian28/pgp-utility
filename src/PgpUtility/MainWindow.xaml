<Window x:Class="PgpUtility.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:PgpUtility.Views"
        xmlns:vm="clr-namespace:PgpUtility.ViewModels"
        xmlns:converters="clr-namespace:PgpUtility.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        Title="PGP Utility" Height="700" Width="1000"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Main TabControl -->
        <TabControl Grid.Row="0" Margin="8"
                    SelectedIndex="{Binding SelectedTabIndex}"
                    Background="Transparent"
                    BorderThickness="0">
            <TabItem Header="Encrypt / Decrypt" Padding="12,6">
                <views:EncryptDecryptView DataContext="{Binding EncryptDecryptVm}" />
            </TabItem>
            <TabItem Header="Key Management" Padding="12,6">
                <views:KeyManagementView DataContext="{Binding KeyManagementVm}" />
            </TabItem>
            <TabItem Header="Key Generation" Padding="12,6">
                <views:KeyGenerationView DataContext="{Binding KeyGenerationVm}" />
            </TabItem>
        </TabControl>

        <!-- Log Panel -->
        <Border Grid.Row="1"
                Visibility="{Binding IsLogPanelVisible, Converter={StaticResource BoolToVis}}"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                MaxHeight="150">
            <ListBox ItemsSource="{Binding LogEntries}"
                     FontFamily="Consolas" FontSize="11"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto" />
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,6">
            <DockPanel>
                <Button DockPanel.Dock="Right"
                        Content="Log"
                        Style="{StaticResource SecondaryButton}"
                        Padding="8,4"
                        Click="ToggleLogPanel_Click" />
                <TextBlock Text="{Binding StatusMessage}"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
